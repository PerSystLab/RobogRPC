syntax = "proto3";

package handcontrol;

message HandData {
  repeated int32 finger_values = 1;
  int64 timestamp_ms = 2;
}

message Ack {
  bool success = 1;
}

service HandController {
  rpc StreamHandData(stream HandData) returns (Ack);
}
